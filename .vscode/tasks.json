{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "setup_python_env",
            "type": "shell",
            "linux": {
                "command": "export CARB_APP_PATH=isaac_install_path/kit && export ISAAC_PATH=isaac_install_path && export EXP_PATH=isaac_install_path/apps && source isaac_install_path/setup_python_env.sh && printenv >isaac_install_path/.vscode/.standalone_examples.env"
            }
        }
    ]
}
